'use client';

interface StoreHeaderProps {
  stores: any[];
}

export const StoreHeader = ({ stores }: StoreHeaderProps) => {
  // This component is no longer needed since store switcher moved to top bar
  return null;
};